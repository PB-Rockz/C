InserttoBST(node *root,val){
    // if root is null, create a new node and return it
    return root;
    Tree node* cur = root;
    while (true)
    {
        if(cur->val<=val){
            if(cur->right!NULL){
                cur=cur->right;
            }
            else {
                cur->right = d allocate
                break;
            }
        }
        else{
            
        }
    }
    
}